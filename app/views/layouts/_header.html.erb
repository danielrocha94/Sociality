<header class="navbar navbar-fixed-top navbar-custom">
  <div class="container">
    <%= link_to "Posts", root_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li>
          <li class="dropdown">
              <a href="#" class="name dropdown-toggle" data-toggle="dropdown">

                <%= gravatar_for(current_user, {size: 20, style:'tiny'})%>
                <%= "#{current_user.first_name} " %><b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                  <li> <%= link_to "Notifications", "#"%></li>
                  <li> <%= link_to "Messages", "#"%>
                  <li> <%= link_to "Friend Requests", "#"%>
              </ul>
            </li>
            <li>
              <%= link_to "Users", users_path %>
            </li>
        <% end %>
        <li><%= link_to "Help", help_path %></li>
        <li><%= link_to "About", about_path %></li>
        <li><%= link_to "Contact", contact_path %></li>
        <% if user_signed_in? %>
          <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            Account <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <li> <%= link_to "My Profile", current_user %></li>
            <li> <%= link_to "Settings", edit_user_registration_path%></li>
            <li class="divider"></li>
            <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
          </ul>

          <li> <%= link_to "New post +", new_post_path,{class: "btn btn-default right"} %></li>
        <% else %>
        <li><%= link_to "Login", new_user_session_path %> </li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>
